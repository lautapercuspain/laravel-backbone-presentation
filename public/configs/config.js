/**
 *
 * Provides an object to load config files by the configHandler.
 *
 */
define(function () {
    var config = {
        'configFiles': {
            'env'   : 'configs/env',
            'errors' : 'configs/errors'
        }
    };

    return config;
});